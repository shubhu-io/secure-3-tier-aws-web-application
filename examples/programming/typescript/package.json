{
  "name": "typescript-http-server",
  "version": "1.0.0",
  "description": "Minimal HTTP server in TypeScript using built-in modules",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "test": "tsc && npx tsc test/index.test.ts --module ESNext --target ES2022 --moduleResolution node --esModuleInterop --outDir dist-test && node --test dist-test/index.test.js",
    "watch": "tsc --watch"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.5.2"
  }
}
